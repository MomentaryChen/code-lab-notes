# Contracts: 002-all-design-patterns

本 feature 為靜態內容擴充（設計模式 hub 依分類列出所有模式、各模式專頁），無後端 API 或伺服端介面。

**無需定義**：REST/GraphQL 端點、OpenAPI 規格、或任何 client–server 合約。

若未來設計模式區塊改為動態載入內容或需後端 API，再於當時 feature 的 `contracts/` 下新增對應規格。
