# Contracts: 001-design-pattern-singleton

本 feature 為靜態內容頁（設計模式主軸、hub、Singleton 說明頁），無後端 API 或伺服端介面。

**無需定義**：REST/GraphQL 端點、OpenAPI 規格、或任何 client–server 合約。

若未來設計模式區塊擴充為需呼叫後端（例如動態載入內容），再於當時 feature 的 `contracts/` 下新增對應規格。
